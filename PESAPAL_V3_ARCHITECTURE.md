# Pesapal v3 Architecture Diagram & Reference

## System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         SIMBA EXPRESS FRONTEND                       │
│                            (index.html)                              │
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 1. Customer fills checkout form                             │   │
│  │ 2. Clicks "💳 Lipa Kwa Simu"                               │   │
│  │ 3. Frontend calls: POST /api/pesapal/submit-order          │   │
│  └──────────────────────┬──────────────────────────────────────┘   │
│                         │                                            │
└─────────────────────────┼────────────────────────────────────────────┘
                          │
                          ↓ (HTTPS)
┌─────────────────────────────────────────────────────────────────────┐
│                    VERCEL BACKEND INFRASTRUCTURE                     │
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ /api/pesapal/submit-order (POST)                            │  │
│  │                                                              │  │
│  │ 1. Calls /api/pesapal/auth to get Pesapal token           │  │
│  │ 2. Submits order to Pesapal API                           │  │
│  │ 3. Creates payment record in Supabase                     │  │
│  │ 4. Returns checkout URL                                   │  │
│  └───────────────────────┬────────────────────────────────────┘  │
│                          │                                         │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ /api/pesapal/auth (GET)                                     │  │
│  │ - Gets Bearer token from Pesapal                           │  │
│  │ - 1-hour expiration                                        │  │
│  │ - Auto-regenerates on demand                              │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                       │
└──────────────────────────────────────────────────────────────────────┘
                          │
                          │ (Checkout URL)
                          ↓
┌─────────────────────────────────────────────────────────────────────┐
│                      PESAPAL CHECKOUT FLOW                           │
│                                                                       │
│  Customer redirected to:                                            │
│  https://cybqa.pesapal.com/api/Merchants/...                       │
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 1. Customer selects payment method                         │   │
│  │    - M-Pesa (🟢)                                           │   │
│  │    - Airtel Money (🔴)                                     │   │
│  │    - Tigopesa (🟡)                                         │   │
│  │    - Halopesa (🟣)                                         │   │
│  │                                                             │   │
│  │ 2. Enters phone number                                     │   │
│  │                                                             │   │
│  │ 3. Gets USSD prompt or app redirect                       │   │
│  │                                                             │   │
│  │ 4. Enters M-Pesa/Airtel PIN on phone                      │   │
│  │                                                             │   │
│  │ 5. Payment processes                                       │   │
│  │                                                             │   │
│  │ 6. Pesapal confirms COMPLETED or FAILED                   │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                       │
└──────────────────────────────────────────────────────────────────────┘
                          │
         ┌────────────────┴────────────────┐
         │                                 │
         ↓ (IPN Webhook)                   ↓ (Redirect)
┌─────────────────────┐         ┌──────────────────────┐
│ /api/pesapal/ipn    │         │ /payment/pending     │
│ (Source of Truth)   │         │ (UX Only)            │
│                     │         │                      │
│ 1. Receives IPN     │         │ Shows loading...     │
│ 2. Verifies with    │         │ Polls for status     │
│    Pesapal          │         │                      │
│ 3. Updates DB:      │         │ Redirects to:        │
│   - payments        │         │ /payment-success     │
│   - orders          │         │ OR                   │
│ 4. Creates audit    │         │ /payment-failed      │
│    log              │         │                      │
└─────────────────────┘         └──────────────────────┘
         │
         ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    SUPABASE DATABASE UPDATES                         │
│                                                                       │
│  payments table:                                                     │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │ id | order_id | status | pesapal_tracking_id | amount       │   │
│  ├──────────────────────────────────────────────────────────────┤   │
│  │... | ord-123  | SUCCESS| txn-456            | 50000         │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                       │
│  orders table:                                                       │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │ id | order_status | total_amount | created_at              │   │
│  ├──────────────────────────────────────────────────────────────┤   │
│  │... | paid         | 55000       | 2026-01-28...           │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                       │
│  payment_logs table:                                                 │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │ id | event_type | status | order_id | raw_data             │   │
│  ├──────────────────────────────────────────────────────────────┤   │
│  │... | IPN_RECEIVED| COMPLETED | ord-123 | {...}            │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                       │
└──────────────────────────────────────────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────────────────────────────────┐
│              ADMIN DASHBOARD ORDER ROUTING                           │
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │ Admin polls every 30 seconds                                │   │
│  │ Finds orders where status = 'paid'                         │   │
│  │                                                              │   │
│  │ Runs Matchmaker Engine:                                    │   │
│  │ - Gets customer location (GPS)                             │   │
│  │ - Calculates distance to merchants                         │   │
│  │ - Selects nearest merchant (5km radius)                   │   │
│  │                                                              │   │
│  │ Updates order:                                             │   │
│  │ status = 'sent_to_merchant'                               │   │
│  │ assigned_merchant_id = best_merchant                      │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                       │
└──────────────────────────────────────────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────────────────────────────────┐
│              MERCHANT RECEIVES NOTIFICATION                          │
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │ Merchant App (Simba-Merchant-App)                           │   │
│  │                                                              │   │
│  │ Real-time notification received:                           │   │
│  │ - Order: Simba Express Order #abc123                      │   │
│  │ - Total: 55,000 TZS                                       │   │
│  │ - Status: PAID ✅                                         │   │
│  │ - Location: GPS coordinates                               │   │
│  │ - Distance: 2.3 km away                                   │   │
│  │                                                              │   │
│  │ Merchant options:                                          │   │
│  │ [Accept] [Decline] [Contact Customer]                     │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                       │
└──────────────────────────────────────────────────────────────────────┘
```

## Data Flow Timeline

```
T+0s    Order created in Supabase
        └─ status: 'awaiting_payment'

T+0.1s  Frontend calls /api/pesapal/submit-order
        └─ Creates payment record (PENDING)

T+0.2s  Backend authenticates with Pesapal
        └─ Gets Bearer token

T+0.3s  Backend submits order to Pesapal
        └─ Pesapal returns checkout URL

T+0.4s  Frontend redirects customer to Pesapal
        └─ Customer browser leaves your site

T+10s   Customer completes M-Pesa payment on phone
        └─ Pesapal verifies with M-Pesa

T+15s   Pesapal sends IPN webhook notification
        └─ POST /api/pesapal/ipn

T+15.1s Backend receives IPN
        └─ Verifies with Pesapal API

T+15.2s Backend updates Supabase:
        ├─ payments.status = SUCCESS
        ├─ orders.order_status = paid
        └─ Creates audit log

T+30s   Admin polling activates
        ├─ Finds 'paid' orders
        ├─ Runs Matchmaker Engine
        ├─ Selects nearest merchant
        └─ Updates order status: 'sent_to_merchant'

T+32s   Merchant receives push notification
        └─ Shows order details

T+45s   Merchant accepts order
        └─ status: 'accepted'

T+120s  Merchant marks ready for delivery
        └─ status: 'ready_for_dispatch'

T+150s  Driver picks up order
T+600s  Order delivered
        └─ status: 'delivered'
```

## API Endpoints Reference

### Authentication
```
GET /api/pesapal/auth

Response:
{
  "token": "eyJhbGciOi...",
  "expiresIn": 3600
}
```

### Submit Order
```
POST /api/pesapal/submit-order

Request:
{
  "orderId": "uuid",
  "amount": 50000,
  "email": "customer@example.com",
  "phone": "255654321098",
  "paymentMethod": "mpesa"
}

Response:
{
  "success": true,
  "checkoutUrl": "https://cybqa.pesapal.com/...",
  "orderTrackingId": "uuid"
}
```

### IPN Webhook
```
POST /api/pesapal/ipn

Received from Pesapal:
{
  "OrderTrackingId": "uuid",
  "OrderMerchantReference": "order-uuid",
  "pesapal_transaction_tracking_id": "TXN-123"
}

Response:
{
  "success": true,
  "message": "IPN processed",
  "orderId": "order-uuid"
}
```

## Payment Status Diagram

```
┌─────────────┐
│   PENDING   │  Order created in Supabase
│ (Payment)   │  Payment record created
└──────┬──────┘
       │
       ↓
┌─────────────────┐
│   PROCESSING    │  User redirected to Pesapal
│ (Pesapal)       │  Entering PIN
└──────┬──────────┘
       │
       ├──→ ┌─────────┐
       │    │ SUCCESS │  IPN received & verified
       │    │ (PAID)  │  Order routed to merchant
       │    └─────────┘
       │
       └──→ ┌─────────┐
            │ FAILED  │  Payment declined
            │ (Error) │  Show retry option
            └─────────┘
```

## Order Status Diagram

```
┌──────────────────┐
│  AWAITING_       │  Customer hasn't paid
│  PAYMENT         │
└────────┬─────────┘
         │
         ↓ (After IPN SUCCESS)
┌──────────────────┐
│  PAID            │  Payment confirmed
│                  │  Ready for routing
└────────┬─────────┘
         │
         ↓ (Admin polling)
┌──────────────────┐
│  SENT_TO_        │  Order assigned to merchant
│  MERCHANT        │  Merchant notified
└────────┬─────────┘
         │
         ├──→ ┌────────────┐
         │    │ DECLINED   │  Merchant declined
         │    │ or         │
         │    │ TIMEOUT    │
         │    └────────────┘
         │
         ↓
┌──────────────────┐
│  ACCEPTED        │  Merchant accepted
│                  │  Preparing order
└────────┬─────────┘
         │
         ↓
┌──────────────────┐
│  READY_FOR_      │  Order ready for pickup
│  DISPATCH        │  Waiting for driver
└────────┬─────────┘
         │
         ↓
┌──────────────────┐
│  DISPATCHED      │  Driver has order
│  or              │  En route
│  IN_TRANSIT      │
└────────┬─────────┘
         │
         ↓
┌──────────────────┐
│  DELIVERED       │  Customer received
│                  │  Order complete
└──────────────────┘
```

## Security Model

```
┌─────────────────────────────────────────────┐
│  FRONTEND (Browser)                         │
├─────────────────────────────────────────────┤
│ ✅ Can see: Payment UI, success message    │
│ ✅ Can access: Public API endpoints        │
│ ❌ Can NOT see: API credentials            │
│ ❌ Can NOT access: Pesapal directly        │
└────────────────┬──────────────────────────┘
                 │ (HTTPS only)
                 ↓
┌─────────────────────────────────────────────┐
│  BACKEND (Vercel)                          │
├─────────────────────────────────────────────┤
│ ✅ Has: PESAPAL_CONSUMER_KEY/SECRET        │
│ ✅ Has: SUPABASE_SERVICE_ROLE_KEY          │
│ ✅ Verifies: IPN signatures                │
│ ✅ Logs: All transactions                  │
│ ❌ Exposes: Nothing to frontend            │
└────────────────┬──────────────────────────┘
                 │ (OAuth/HTTPS)
                 ↓
┌─────────────────────────────────────────────┐
│  PESAPAL API (Live)                        │
├─────────────────────────────────────────────┤
│ Receives: Bearer token + order data        │
│ Returns: Checkout URL                      │
│ Sends: IPN webhook notifications          │
└─────────────────────────────────────────────┘
```

## Environment Variables

```
# .env file (NEVER commit)

# Pesapal Credentials
PESAPAL_CONSUMER_KEY=MgnBl7oCT2JmzK8MQ9fP6p5VVVh5rrg0
PESAPAL_CONSUMER_SECRET=tnVjvF7eUCNBLcamk/dylBi8CIs=
PESAPAL_IPN_ID=your_registered_ipn_id

# API Endpoints
PESAPAL_API_URL=https://cybqa.pesapal.com/pesapalv3/api
PESAPAL_TOKEN_ENDPOINT=https://cybqa.pesapal.com/pesapalv3/api/Auth/RequestToken
PESAPAL_SUBMIT_ORDER_ENDPOINT=https://cybqa.pesapal.com/pesapalv3/api/Orders/SubmitOrder
PESAPAL_VERIFY_ORDER_ENDPOINT=https://cybqa.pesapal.com/pesapalv3/api/Orders/GetOrderDetails

# App URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
PESAPAL_CALLBACK_URL=http://localhost:3000/payment/pending
PESAPAL_FAILURE_URL=http://localhost:3000/payment/failed

# Supabase
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
```

---

This diagram covers the complete payment flow from start to finish!
